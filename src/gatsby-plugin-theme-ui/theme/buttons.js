import links from './links'

export default {
  'color-mode-toggle': {
    ...links.nav,
    display: 'inline-flex',
    borderRadius: 0,
    backgroundColor: 'transparent',
    color: 'text',
    cursor: 'pointer',
  },
}
