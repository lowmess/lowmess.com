---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h4">;

const { class: className, ...props } = Astro.props;
---

<h4 class:list={["heading-four", className]} {...props}><slot /></h4>

<style>
	.heading-four {
		margin-block-start: var(--rhythm-md);
		margin-block-end: var(--rhythm-md);
		font-family: var(--font-family-hanken-grotesk);
		font-size: var(--font-size-lg);
		font-weight: 500;
		text-wrap: balance;

		@media (min-width: 30em) {
			margin-block-start: var(--rhythm-lg);
		}
	}

	:global(h4.heading-four code) {
		font-size: var(--font-size-md);
	}
</style>
